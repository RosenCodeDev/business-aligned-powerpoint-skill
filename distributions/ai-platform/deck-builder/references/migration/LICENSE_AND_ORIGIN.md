# License and origin

This document reports only origin/licensing information that can be supported by the accessible files or the current verification environment. It is **not legal advice** and does not infer ownership or permission from the mere fact that the files are accessible.

## Summary

| Resource | Known origin from accessible material | License / reuse status verified here? | Notes |
|---|---|---|---|
| `ow_default.pptx` | Described by the runtime/knowledge files as an Oliver Wyman-branded template | **No** | No standalone license or reuse grant is bundled |
| Oliver Wyman brand rules/palette/layouts | Encoded in exact knowledge files and runtime | **No** | Technical extraction does not establish trademark/brand-use permission |
| Marsh Serif | Referenced as major/primary font by template/runtime | **No** | No standalone font file or license text exported |
| Noto Sans | Referenced as minor/body font by template/runtime | **No, from supplied package** | No standalone font license text is bundled in the runtime |
| Embedded `ppt/fonts/*.fntdata` | Six embedded font data parts inside `ow_default.pptx` | **No** | PowerPoint embedded-font rights/obfuscation are not documented in the package |
| Curated icon set | 500 SVGs under `src/assets/icons/`; described as curated OW line icons | **No** | No icon license/attribution file is present; original icon provider is unavailable |
| Runtime source code | Bundled under `src/` in `deck_system_runtime_clean.zip` | **No** | No top-level LICENSE/NOTICE/COPYING file is present in the archive |
| `svgelements` vendored source | `src/svgelements/`, reports VERSION 1.9.6 | **No, from supplied archive** | No bundled license file was found; verify upstream before redistribution |
| python-pptx | Third-party Python dependency | Current environment metadata says MIT | Upstream package, not bundled as source in runtime archive |
| lxml | Third-party Python dependency | Current environment metadata says BSD-3-Clause | Upstream package |
| Pillow | Third-party Python dependency | Current environment metadata reports MIT-CMU | Upstream package |
| pytest | Third-party Python dependency | Current environment metadata says MIT | Test dependency |
| openpyxl | Third-party Python dependency | Current environment metadata says MIT | Upstream package |
| fonttools | Third-party Python dependency | Current environment metadata says MIT | Upstream package |
| PyYAML | Third-party Python dependency | Current environment metadata says MIT | Upstream package |
| Migration/verification Markdown generated in this conversation | Generated by this assistant from accessible owner material and runtime facts | Not an upstream original | Clearly marked generated/reconstructed where applicable |

## Presentation template and branding

The exact source material describes `ow_default.pptx` as an **Oliver Wyman-branded template** carrying:

- Marsh Serif heading/major font,
- Noto Sans body/minor font,
- Oliver Wyman palette,
- master layouts,
- chart/table styling,
- logos/footers where present in the template.

No license, authorization letter, trademark permission, or redistribution terms for that template are present in the supplied runtime archive.

Therefore this verification package does **not** assert:

- that OpenAI owns the template,
- that the assistant owns the template,
- that the template may be redistributed outside the owner's authorized context,
- or that Oliver Wyman branding may be used by third parties.

A human owner should verify internal rights before redistributing or deploying the branded assets.

## Fonts

The runtime references Marsh Serif and Noto Sans by family name and the template contains six `ppt/fonts/*.fntdata` parts.

What is not available:

- standalone Marsh Serif font files,
- standalone Noto Sans font files,
- font EULAs or embedding-rights documentation,
- confirmation that the embedded `.fntdata` may be extracted or redistributed.

This package intentionally does not export font files.

For a local deployment, use lawfully installed fonts or approved substitutes and preserve PowerPoint embedding restrictions.

## Icon collection

The runtime contains exactly 500 SVG icon files in `src/assets/icons/`.

The knowledge file `23_icon_index.md` calls this a curated OW icon set and says the runtime renders icons as native editable freeforms. The SVG files contain no reliable license attribution, and the archive contains no LICENSE/NOTICE file for the icon set.

The original provider and redistribution rights are therefore **unavailable** from the accessible material. Do not assume that "Heroicons-style names" in the resolver means the underlying SVG artwork is licensed as Heroicons; that would be an unsupported inference.

## Runtime source code

The runtime archive contains 31 Python files, including the generator, compiler, runtime, gate, design, voice, image, icon, infographic, autolayout, and test-support modules.

No top-level source-code license file was found in the archive. The user has stated they own or are authorized to migrate this GPT, but that statement is not sufficient for this document to independently establish copyright ownership or downstream redistribution rights for every bundled component.

A human owner should identify the internal repository/license record for the runtime before broad redistribution.

## Third-party Python dependencies

The exact runtime requirements are:

```text
python-pptx>=0.6.23
lxml>=5.0.0
Pillow>=10.0.0
pytest>=8.0.0
openpyxl>=3.1
svgelements>=1.9
fonttools>=4.40
pyyaml
```

The runtime archive does not bundle license texts for these packages.

The current verification environment's installed package metadata reports:

| Package | Observed version | Metadata license field |
|---|---:|---|
| python-pptx | 1.0.2 | MIT |
| lxml | 6.1.1 | BSD-3-Clause |
| Pillow | 12.3.0 | MIT-CMU |
| pytest | 9.0.2 | MIT |
| openpyxl | 3.1.5 | MIT |
| fonttools | 4.63.0 | MIT |
| PyYAML | 6.0.3 | MIT |

This table is environment metadata, not a substitute for reviewing the license text of the exact versions installed in a deployment.

`svgelements` is special: the runtime contains vendored source reporting VERSION 1.9.6, but no license file for that vendored copy was found. Its upstream reuse status should be verified manually.

## Web, image generation, and OpenAI services

No API credential, authentication token, or provider license is included in the runtime or verification package.

Use of web search, image generation, or other hosted services is governed by the terms of the service configured by the deploying organization. This verification package does not grant rights to any such service.

## Feedback URL

The owner-authored instructions contain:

`https://owy.mn/4v3C4gV`

Its ownership, telemetry behavior, retention policy, and continued appropriateness outside the original Custom GPT are not verifiable from the supplied files. A local skill should not automatically preserve it unless the owner confirms that it remains desired and authorized.

## Recommended manual checks before redistribution

1. Confirm internal ownership/licensing of `deck_system_runtime_clean.zip` source code.
2. Confirm authorization to redistribute `ow_default.pptx`.
3. Confirm Marsh Serif licensing and PowerPoint embedding rights.
4. Confirm provenance/license of the 500 SVG icons.
5. Confirm the vendored `svgelements` license.
6. Preserve upstream licenses/notices for installed third-party Python packages.
7. Confirm whether the Oliver Wyman name, visual identity, and feedback URL should remain in a local or external Agent Skill.
